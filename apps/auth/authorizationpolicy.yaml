apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-to-auth
  namespace: zt-auth
spec:
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/zt-auth/sa/waypoint"
